apiVersion: v1
kind: Service
metadata:
  name: {{ include "convos.fullname" . }}
  labels:
    {{- include "convos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "convos.selectorLabels" . | nindent 4 }}
