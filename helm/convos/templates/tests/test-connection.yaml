apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "convos.fullname" . }}-test-connection"
  labels:
    {{- include "convos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "convos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
