# Helm Chart for convos

To deploy this helm chart to your current kubernetes cluster with helm:

  helm install app ./convos

Look at convos/values.yaml for the default values available for overriding.
