{
  redis => join('/',
    $ENV{DOTCLOUD_REDIS_REDIS_URL} || 'redis://127.0.0.1:6379',
    $ENV{REDIS_DATABASE_INDEX} || 1,
  ),
  name => 'Nordaaker',
  secret => 'some-random-secret-string', # need to be unique pr installation
  default_connection => {
    host => 'irc.perl.org',
    channels => '#wirc',
  },
  hypnotoad => {
    listen => ['https://*:6667']
  },
}
