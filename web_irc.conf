{
  name => 'Nordaaker',
  redis => 'redis://127.0.0.1:6379/1',
  secret => 'some-random-secret-string', # need to be unique pr installation
  backend => {
    proxy => 0, # switch to 1 to enable standalone client to wirc proxy
    embedded => 0, # set to 1 if running just a single mojo daemon
  },
  default_connection => {
    server => 'irc.perl.org',
    channels => '#wirc',
  },
  hypnotoad => {
    listen => ['http://*:5000']
  },
}
