{
  # Name of your organization
  name => 'Nordaaker',

  # Production config for the hypnotoad web server
  hypnotoad => {
    listen => [qw( http://*:8080 )],
    proxy => $ENV{CONVOS_PROXY} // 0,
  },
}
