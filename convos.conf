{
  name => 'Nordaaker',
  redis => 'redis://127.0.0.1:6379/1',
  # Used for client side cookies
  secret => 'some-random-secret-string', # need to be unique pr installation
   #invite_code => 'nordaaker', # if set, must be appended to register url
  # for example /register/nordaaker
  backend => {
    proxy => 0, # switch to 1 to enable standalone client to convos proxy
    embedded => 0, # set to 1 if running just a single mojo daemon
  },
  default_connection => {
    server => 'irc.perl.org',
    channels => '#convos',
  },
  hypnotoad => {
    listen => ['http://*:5000']
  },
}
