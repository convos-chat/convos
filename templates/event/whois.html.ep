<li class="whois" data-host="<%= $host %>" data-target="any">
  <div class="content">
    %= link_to $nick, view => { host => $host, target => $nick }
    is <%= $user %>@<%= $host %> (<%= $realname %>).
  </div>
  %= timestamp_span($timestamp)
</li>
