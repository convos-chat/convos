<li class="topic" data-cid="<%= $cid %>" data-target="<%= $target %>">
% if(length $topic) {
  <span class="prefix">Topic is</span>
  <span><%= $topic %></span>
% } else {
  <span class="prefix">No topic is set for <%= $target %>.</span>
% }
</li>
