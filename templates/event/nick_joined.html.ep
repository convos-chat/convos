<li class="nick-joined" data-host="<%= $host %>" data-target="<%= $target %>">
  <div class="content"><%= link_to $nick, 'channel.view', { host => $host, target => $nick }, class => 'nick' %></b> joined <%= $target %></div>
  %= timestamp_span($timestamp)
</li>
