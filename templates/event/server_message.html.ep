<li class="server-message" data-target="<%= $cid ? as_id $cid : 'any' %>">
  <span><%= $status < 400 ? 'Notice' : 'Error' %> - <%= $message %></span>
% if(my $timestamp = stash 'timestamp') {
  %= timestamp_span($timestamp)
% }
</li>
