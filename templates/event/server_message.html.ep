<li class="network-message <%= $status < 400 ? 'notice' : 'error' %>" data-network="<%= $network %>" data-target="any">
  <div class="content"><%== $message %></div>
  %= timestamp_span($timestamp || time)
</li>
