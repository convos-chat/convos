<li class="muted" data-target="target_<%=$cid%>">
  <span><%= $status < 400 ? 'Notice' : 'Error' %> - <%= $message %></span>
%if($self->stash('timestamp')) {
  <span class="timestamp"><%= format_time $timestamp, "%H:%M:%S" %></span>
%}
</li>
