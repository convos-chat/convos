<li class="server-message <%= $status < 400 ? 'notice' : 'error' %>" data-host="<%= $host %>" data-target="any">
  <div class="content"><%== $message %></div>
  %= timestamp_span($timestamp || time)
</li>
