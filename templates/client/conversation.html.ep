<ul class="unstyled chat-messages" id="conversation_<%= as_id $cid, $target %>" data-nick="<%= $nick %>" data-nicks="<%= join ',', @$nicks %>">
% for my $message (@$conversation) {
    %= include 'event/'.$message->{event}, %$message
% }
</ul>
