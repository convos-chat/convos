    % if(@$connections) {
      <ul class="unstyled" id="chat-messages"
        data-cid="<%= $cid %>"
        data-nicks="<%= join ',', @$nicks %>"
        data-target="<%= $target %>">
    % }
    % my $n = 0;
    % for my $message (@$conversation) {
      %= include 'event/'.$message->{event}, %$message
    % }
      </li>
%== qq{</ul>} if @$connections
