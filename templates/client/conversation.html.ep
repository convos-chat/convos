<ul id="conversation_<%= as_id $cid, $target %>" data-nick="<%= $nick %>" data-offset="<%= @$conversation ? int $conversation->[0]{timestamp} : time %>">
% for my $message (@$conversation) {
    %= include "event/$message->{event}", highlight => 0, %$message
% }
</ul>
